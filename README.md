```bash
# target nodejs version = 18.18.2
git clone https://github.com/skku-algorithms-team5/oakathon-front.git
cd oakathon-front
npm install
npm run build
npx serve -s build -l 3000
# http://localhost:3000로 접근
```
